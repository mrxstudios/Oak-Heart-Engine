#pragma once

/*
#include <string>
#include <sol/sol.hpp> // Include the scripting library's header

class ScriptingManager {
public:
	ScriptingManager();
	~ScriptingManager();

	void LoadScript(const std::string& filename);
	void RunScriptFunction(const std::string& functionName);

	// Additional methods for interacting with the scripting system

private:
	sol::state luaState; // The scripting state
};*/
