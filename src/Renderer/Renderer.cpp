#include "Renderer.h"
/*
Renderer::Renderer(SDL_Window* window) {
	sdlRenderer = SDL_CreateRenderer(window, NULL, SDL_RENDERER_ACCELERATED);
	if (!sdlRenderer) {
		// Handle error
	}
}

Renderer::~Renderer() {
	SDL_DestroyRenderer(sdlRenderer);
}

void Renderer::Clear() {
	SDL_SetRenderDrawColor(sdlRenderer, 0, 0, 0, 255); // Set clear color to black
	SDL_RenderClear(sdlRenderer);
}

void Renderer::Present() {
	SDL_RenderPresent(sdlRenderer);
}

// Additional method implementations for rendering game elements, setting up the viewport, etc.
*/