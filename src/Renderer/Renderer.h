#pragma once
/*
#include <SDL3/SDL.h>

class Renderer {
public:
	Renderer();
	Renderer(SDL_Window* window);
	~Renderer();

	void Clear();
	void Present();

	// Additional methods for rendering game elements, setting up the viewport, etc.

private:
	SDL_Renderer* sdlRenderer;
};
*/